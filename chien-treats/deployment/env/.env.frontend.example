# Frontend Environment Variables
# Copy to /srv/sites/chienstreats/shared/.env.frontend

# Next.js
NODE_ENV=production
PORT=3101
HOSTNAME=0.0.0.0

# API connection (same-origin via Nginx reverse proxy)
NEXT_PUBLIC_API_BASE_URL=https://chienstreats.com/api/v1
